// TODO add code
#include "utils.h"

// layerx_gpu_cuda(cuda, cuda);
// layerx_gpu_cuda(cuda, cuda);
// layerx_gpu_cuda(cuda, cuda);
// layerx_gpu_cuda(cuda, cuda);
// layerx_gpu_cuda(cuda, cuda);
// layerx_gpu_cuda(cuda, cuda);
// layerx_gpu_cuda(cuda, cuda);
